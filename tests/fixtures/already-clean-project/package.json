{
  "name": "already-clean-project-fixture",
  "version": "1.0.0",
  "description": "Test fixture for verifying cleanup doesn't break clean code",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}

