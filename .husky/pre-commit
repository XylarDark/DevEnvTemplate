#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Lint-staged disabled - run 'npm run format' manually before commits
# npx lint-staged

# Type check website if it exists
if [ -d "website" ]; then
  cd website
  npx tsc --noEmit
  cd ..
fi

# Run basic tests (if any)
# npm test 2>/dev/null || true

echo "✓ Pre-commit checks passed (lint-staged disabled)"
exit 0
