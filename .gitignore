# Secrets: never commit real environment files. Templates (.env.example) are tracked.
.env
.env.*
!.env.example
!.env.*.example

# DevEnv reports and generated files (project-specific, preserved during sync)
# These files are generated by the doctor command and are project-specific
stack-report.json
gaps-report.md
gaps-report.json
health-report.json
health-before.json
health-after.json
input.txt
*.log

# Keep structure but ignore generated content

# Build artifacts
dist/
node_modules/
tsconfig.tsbuildinfo
*.tsbuildinfo
